extends layout

block content
  h1=title

  form(method='POST')
    div.form-group
      label(for='name') Name:
      input#first_name.form-control(type='text', placeholder='Madonna' name='name' required  )
 
    button.btn.btn-primary(type='submit') Submit
    a(href=artist.url) Cancel

  if errors
    ul
        li!= error.msg